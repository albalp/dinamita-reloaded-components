import '../css/header.css'
const Header = ({borderRadius, label, size, ...props}) =>{
    return(
        <div>
            <header className="header">
                Create Activity
            </header>
        </div>
    );
}
export default Header;